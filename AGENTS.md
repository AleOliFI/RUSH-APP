# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Desenvolvimento Diário

O workflow automatizado foi removido (exigia um `ANTHROPIC_API_KEY` que não temos).
O processo continua válido para execução manual:
- `docs/DAILY_PROMPT.md` — as instruções completas para cada sessão
- `docs/DAILY_AGENDA.md` — log acumulado do que já foi implementado

**Sempre leia `docs/DAILY_AGENDA.md` antes de começar qualquer trabalho** para não reimplementar features existentes.
